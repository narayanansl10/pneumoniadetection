The Code is split into two parts. Test and train. The train part contains the architecture of the neural network constructed and the test file prints the classification with accuracy. The dataset is taken from the link in the dataset file and found to have 98.7% accuracy.
The Three sets are given to classify between the colour and grayscale variants.
The set of train2 gives the highest accuracy
